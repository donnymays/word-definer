<h1>Word Name: <%= @word.name%></h1>
<h2>Definitions:</h2>
<% if @definitions.any? %>
    <ul>
      <% @word.definitions.each do |defintion| %>
        <li><a href="/words/<%= @word.id %>/definition/<%= defintion.id %>"><%= defintion.name %></a></li>
      <% end %>
    </ul>
  <% else %>
    <p>There are no definitionsS listed for this word yet.</p>
  <% end %>

<h4>Add a definition:</h4>

<form action="/words/<%= @word.id %>/definition" method="post">
  <div class="form-group">
    <label for="definition_name">definition</label>
    <input id="definition_name" name="definition_name" class="form-control" type="text">
  </div>
  <button type="submit" class="btn">Add definition</button>
</form>


<p><a href="/words/<%= @word.id %>/edit">Edit word</a></p>
<a href="/words">Return to word list</a>